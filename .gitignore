# SECRETS — never commit (no inline comments: gitignore treats them as part of the pattern)
ssh/
SECRETS.handoff.txt
signing-key*.json
**/.env
**/.env.*
!**/.env.example
**/blink-private.pem
**/agents.json
**/*.key
**/wallets.json

# deps / build
node_modules/
**/node_modules/
dist/
build/
.next/
out/
*.tsbuildinfo
coverage/

# Foundry (contracts)
**/cache/
**/broadcast/**/dry-run/
forge-cache/

# handoff artifacts (not part of the product)
justify-handoff/
justify-handoff.zip

# runtime / logs / junk
*.log
**/*.log
.DS_Store
**/__MACOSX/
Thumbs.db
.vscode/
.idea/

# duplicate of docs/build-notes
sandbox-artifacts/

# docs/notes hidden for now (WIP, kept local, untracked)
*.md
docs/

# foundry artifacts
contracts/out/
contracts/broadcast/

# backend runtime state
backend/data/

# blink sandbox key
**/blink-sandbox-private.pem

# vendored audited Gnosis artifacts are needed by tests/deploy — keep them tracked
!contracts/vendor/out/
!contracts/vendor/out/**
backend/data-arc-backup*/
