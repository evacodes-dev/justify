# RESULTS

| модуль | PASS/PARTIAL/FAIL | минут | ожидание совпало? | грабли/заметки |
|---|---|---|---|---|
| Arc | **PASS** | ~25 | Да, полностью | Стандартный Foundry-флоу работает БЕЗ модификаций. USDC — нативный газ И актив; нативный value-transfer и ERC-20 transfer @0x3600 двигают ОДИН баланс (получатель = 2 USDC после двух переводов по 1). Explorer = Blockscout (v2 API живой). |
| Uniswap | **PASS** | ~15 | Да (+бонус: testnet есть) | Quote ETH→USDC работает на mainnet (0.01 ETH=16.66 USDC) И на Ethereum Sepolia (11155111). Base/Unichain Sepolia вернули пусто. Native ETH = sentinel 0x000..0, permitData=null (для native approve не нужен). Auth: x-api-key + x-universal-router-version:2.0. Исполнение: /swap → calldata → подпись/бродкаст своим кошельком. |
| CRE | **PASS** | ~55 | Да, даже лучше (профильный шаблон) | Симуляция prediction-market-resolution прошла: cron→чтение BTC/USD feed Sepolia ($63125)→resolve 3 рынков→chain-write dry-run (tx 0x000..0). Auth=cre login (браузер). ГРАБЛЯ: cre simulate спавнит `bun` напрямую — npm-global bun (.cmd shim) НЕ находится, нужен реальный bun.exe в PATH. CRE skill склонирован — лечит галлюцинации API. |
| ENS | **PASS** (mainnet) / FAIL (Sepolia) | ~90 | Да на mainnet; Sepolia v1-регистрация закрыта (ENSv2) | Sepolia мигрировал на ENSv2 (живой контроллер не юзает классич. NameWrapper → register ревертит 0x). Перешли на mainnet: субдомен `vadym.jstfy-demo.eth` под `jstfy-demo.eth` выдан + text records, чтение ensjs+viem. Имя НЕ завёрнуто → registry-путь. Газ субдомен+records ≈ $0.15 @0.2 Gwei. tx createSubname 0xfb348c…, setRecords 0x23adc3…. |
| Dynamic | **PASS** | ~20 | Да (login+wallet) | Email/social логин + embedded EVM-кошелёк работают (в showcase виден адрес после логина). Flow (Fireblocks) — enterprise-gated, отдельный апрув (book-a-call), это PARTIAL по Flow. Arc прописан кастомной EVM-сетью в провайдере. |
| World ID | **PASS** | ~40 | Да (4.0) | Целились в World ID 4.0 (текущий дефолт). Флоу: /api/rp-signature (signRequest, secp256k1) → IDKitRequestWidget staging → симулятор → /api/v4/verify/{rp_id} → nullifier получен, reuse-guard 409. Прошло вживую (nullifier 0x10b01f…7a8b). Грабля: классический v2/IDKitWidget теперь legacy; v4 требует rp_id+signing_key+RP-подпись на бэке. |
| Blink | **PARTIAL** | ~30 | Частично | Виджет рендерится и доходит до hosted-флоу (модалка открывается). Реальный депозит блокирован: sandbox/testnet НЕТ (mainnet only) + merchantId (81172dcf…) ждёт ручного аппрува оператора. Подпись по signed-link (P-256). |
